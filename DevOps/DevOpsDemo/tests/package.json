{
  "name": "devops-demo-hmi-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:hmi": "playwright test --config playwright.config.mjs --headed",
    "test:hmi:headless": "playwright test --config playwright.config.mjs",
    "test:demo": "playwright test --config playwright.config.mjs --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}